# Credential Storage

The settings UI will accept a user-provided server URL and API key. Values must never be hardcoded
or logged. When implemented, secrets should use an operating-system-backed credential facility;
documentation and examples must contain placeholders only.
