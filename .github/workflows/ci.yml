name: Windows CI

on:
  pull_request:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: windows-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-and-package:
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Validate pull request title
        if: ${{ github.event_name == 'pull_request' }}
        shell: pwsh
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: |
          $pattern = '^(feat|fix|docs|chore|refactor|perf|test|build|ci|revert|style)(\([a-z0-9._/-]+\))?!?: .+$'
          if ($env:PR_TITLE -notmatch $pattern) {
            throw "Pull request title must use Conventional Commits, for example: feat: add transcript search"
          }

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.6.0
          run_install: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm typecheck

      - name: Test
        run: pnpm test

      - name: Build Windows installer
        run: pnpm package:win

      - name: Upload Windows installer
        uses: actions/upload-artifact@v4
        with:
          name: local-whisper-studio-windows-${{ github.sha }}
          path: release/Local Whisper Studio Setup *.exe
          if-no-files-found: error
          retention-days: 14

  merge-release:
    needs: validate-and-package
    if: >-
      github.event_name == 'pull_request' &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      startsWith(github.head_ref, 'release-please--branches--master--') &&
      startsWith(github.event.pull_request.title, 'chore(master): release ')
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Merge successful release pull request
        env:
          GH_TOKEN: ${{ secrets.RELEASE_AUTOMATION_TOKEN }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          PR_URL: ${{ github.event.pull_request.html_url }}
        run: gh pr merge "$PR_URL" --squash --delete-branch --match-head-commit "$HEAD_SHA"
