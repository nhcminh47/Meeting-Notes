# Samples

Do not commit private or real meeting audio here.

Live engine benchmarks should use either synthetic audio generated locally, a tiny redistributable
sample with a clear license, or a local user-provided WAV path passed to the benchmark CLI. Local
benchmark outputs belong under `benchmark-results/`, which is ignored by git.
